I first imported the required libraries and loaded the given data then cleaned depth values to calculate smoother values and average of nearby data points was used to reduce sudden changes and make the data easier to understand. Then created a graph using Matplotlib to show the depth measurements over time also added an animation to make graph look good.
